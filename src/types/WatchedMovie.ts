// src/types/WatchedMovie.ts
export interface WatchedMovie {
  id: number;
  title: string;
  release_date: string; // or another date format if you prefer
  poster_path: string; // assuming you have a URL to the poster image
  // Add any other properties that are relevant
}
